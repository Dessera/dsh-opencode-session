{
  "name": "dsh-opencode-session",
  "version": "0.1.1-fork.1",
  "description": "Local fork of dsh-opencode-session: force-overrides x-opencode-session on session calls and adds a stable fallback for auxiliary (session-less) calls to the OpenCode gateway. Temporary patch until the pi-ai upstream fix ships in DeepSeek Harness.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "author": {
    "name": "nobu121 (original), local fork"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Dessera/dsh-opencode-session.git"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
